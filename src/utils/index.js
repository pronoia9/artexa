import {
  getSystemTheme,
  getThemeObject,
  getStoredSettings,
  setStoredSettings,
  getThemeFromStorage,
  saveThemeToStorage,
  isDarkTheme,
  lowerCase,
  rem,
  ProgressbarInstance,
  rgbaToHex,
} from './utils';
import { sceneMotion, appMotion, pageWrapperMotion, sectionWrapperMotion, laptopMotion, heroMotion, countersMotion } from './motion';

export {
  getSystemTheme,
  getThemeObject,
  getStoredSettings,
  setStoredSettings,
  getThemeFromStorage,
  saveThemeToStorage,
  isDarkTheme,
  lowerCase,
  rem,
  ProgressbarInstance,
  rgbaToHex,
  // Motion
  sceneMotion,
  appMotion,
  pageWrapperMotion,
  sectionWrapperMotion,
  laptopMotion,
  heroMotion,
  countersMotion,
};
